{
  "name": "Manim Studio",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "postCreateCommand": "sudo apt update && sudo apt install -y libcairo2-dev libpango1.0-dev ffmpeg texlive texlive-latex-extra && pip install manim",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python"]
    }
  }
}
